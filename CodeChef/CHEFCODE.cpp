#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long i,n,k;
    cin >> n >> k;
    multiset <long long> nums;
    for(i=0;i<n;i++)
    {
        long long temp;
        cin >> temp;
        nums.insert(temp);
    }
    long long ans=0;
    for(auto it=nums.begin();it!=nums.end();it++)
    {
        long long cur=*it;
        float
    }
}
